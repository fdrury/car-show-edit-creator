[pytest]
testpaths = tests
python_files = test_*.py
addopts = --tb=short
# smoke.py is the legacy integration test that requires a running server.
# It's not auto-collected because it doesn't match test_*.py.
norecursedirs = fixtures
